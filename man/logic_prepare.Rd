% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api_core.R
\name{logic_prepare}
\alias{logic_prepare}
\title{Prepare REO logic input for LogicComm}
\usage{
logic_prepare(
  object,
  genes = NULL,
  lr_db = lr_pairs_human,
  include_modulators = FALSE,
  rank_threshold = 0.5,
  layer = "counts",
  chunk_size = 5000,
  return_rank = FALSE,
  rank_output = c("percentile", "none"),
  anchor_genes = NULL,
  verbose = TRUE
)
}
\arguments{
\item{object}{Expression matrix, sparse matrix, Seurat object, or BPCells input.}

\item{genes}{Optional genes to retain. If \code{NULL}, genes are extracted from
\code{lr_db}; set both \code{genes = NULL} and \code{lr_db = NULL} to retain
all genes.}

\item{lr_db}{Optional LogicComm ligand-receptor database used to choose retained
genes when \code{genes} is \code{NULL}.}

\item{include_modulators}{Include LR modulator genes when extracting genes from
\code{lr_db}.}

\item{rank_threshold}{Per-cell REO anchor quantile.}

\item{layer}{Seurat assay layer/slot.}

\item{chunk_size}{Cells per processing chunk.}

\item{return_rank}{Return rank-percentile matrix with REO logic.}

\item{rank_output}{Rank output mode.}

\item{anchor_genes}{Optional gene universe for REO anchors.}

\item{verbose}{Print progress messages.}
}
\value{
A REO sparse matrix, or a \code{LogicCommREOResult} when
  \code{return_rank = TRUE}.
}
\description{
Prepare REO logic input for LogicComm
}
