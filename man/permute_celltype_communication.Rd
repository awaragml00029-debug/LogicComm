% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/celltype_stats.R
\name{permute_celltype_communication}
\alias{permute_celltype_communication}
\title{Cell-Label Permutation Null for Cell-Type Communication}
\usage{
permute_celltype_communication(
  ct_comm = NULL,
  reo_mat,
  cell_labels = NULL,
  knn_mat = NULL,
  lr_db = NULL,
  n_perm = 100,
  n_cores = 1L,
  metric = "sum_lcs",
  seed = NULL,
  adaptive = FALSE,
  adaptive_top_n = 25,
  adaptive_n_perm = 1000,
  min_n_perm_publication = 1000,
  verbose = TRUE,
  ...
)
}
\arguments{
\item{ct_comm}{Optional observed object. If NULL, it is computed from inputs.}

\item{reo_mat}{Binary REO matrix or LogicCommREOResult.}

\item{cell_labels}{Named cell-type labels. Defaults to \code{ct_comm$cell_labels}
when \code{ct_comm} is supplied. If \code{ct_comm} was built after filtering
unlabeled cells, \code{reo_mat} and named \code{knn_mat} inputs are aligned to
those retained cells automatically.}

\item{knn_mat}{Optional KNN/SNN graph. Named graphs are subset to retained cells
when necessary.}

\item{lr_db}{LR database. Defaults to \code{ct_comm$lr_db} when available.}

\item{n_perm}{Number of permutations.}

\item{n_cores}{Number of forked worker processes for the permutation loop
(Unix/macOS). Default 1 (serial). Higher values give a near-linear speedup,
making a publication-grade \code{n_perm} affordable; ignored on Windows.}

\item{metric}{Pair-level metric to test.}

\item{seed}{Optional random seed.}

\item{adaptive}{If TRUE, refine top preliminary candidates after the first
\code{n_perm} permutations.}

\item{adaptive_top_n}{Number of preliminary candidates to refine.}

\item{adaptive_n_perm}{Target total permutations for adaptive candidates. For
example, \code{n_perm = 100} and \code{adaptive_n_perm = 1000} runs 900
additional full permutations for the top candidates.}

\item{min_n_perm_publication}{Recommended minimum permutation count.}

\item{verbose}{Print progress, including adaptive-phase elapsed time.}

\item{...}{Passed to \code{summarize_celltype_communication()}.}
}
\value{
Pair-level null summary with empirical p-values.
}
\description{
Cell-Label Permutation Null for Cell-Type Communication
}
\examples{
expr <- matrix(
  c(5, 1, 4, 2, 1, 5, 3, 4, 4, 2, 5, 1),
  nrow = 3,
  dimnames = list(c("L1", "R1", "T1"), paste0("cell", 1:4))
)
reo <- expr >= 3
rank_mat <- apply(expr, 2, rank) / nrow(expr)
lr_db <- data.frame(
  ligand = "L1",
  receptor = "R1",
  pathway = "toy",
  stringsAsFactors = FALSE
)
lcs <- data.frame(
  ligand = "L1",
  receptor = "R1",
  pathway = "toy",
  sample = c("S1", "S2"),
  group = c("control", "case"),
  sender = "A",
  receiver = "B",
  celltype_sender = "A",
  celltype_receiver = "B",
  LCS = c(0.2, 0.5),
  lcs = c(0.2, 0.5),
  mean_lcs = c(0.2, 0.5),
  delta_lcs = c(0.0, 0.3),
  p_value = c(0.5, 0.01),
  p_adj = c(0.5, 0.02),
  fdr = c(0.5, 0.02),
  stringsAsFactors = FALSE
)
sample_ct_list <- list(S1 = lcs, S2 = lcs)
group_info <- c(S1 = "control", S2 = "case")
knn <- matrix(1, nrow = 4, ncol = 4, dimnames = list(colnames(expr), colnames(expr)))
diag(knn) <- 0
toy_args <- list(
  x = lcs, result = lcs, results = lcs, lcs_df = lcs, ct_comm = lcs,
  comm_df = lcs, communication = lcs, celltype_comm = lcs,
  celltype_results = lcs, differential_results = lcs, diff_comm = lcs,
  glm_result = lcs, role_df = lcs, roles = lcs, specificity = lcs,
  null_pair = list(observed = lcs, null = lcs), reo_mat = reo,
  rank_mat = rank_mat, expr_mat = expr, expression = expr,
  lr_db = lr_db, samples = list(S1 = expr, S2 = expr),
  sample_ct_list = sample_ct_list, group_info = group_info,
  group_labels = group_info, groups = group_info, knn_mat = knn,
  output_dir = tempfile("logiccomm"), file = tempfile(fileext = ".csv"),
  path = tempfile(fileext = ".csv")
)
fun <- get("permute_celltype_communication")
toy_args <- toy_args[intersect(names(toy_args), names(formals(fun)))]
try(do.call(fun, toy_args), silent = TRUE)
}
