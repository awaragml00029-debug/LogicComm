% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_multisample.R
\name{run_multisample}
\alias{run_multisample}
\title{Run Full LogicComm Pipeline on Multiple Samples}
\usage{
run_multisample(
  sample_list,
  group_info,
  lr_db = lr_pairs_human,
  rank_threshold = 0.5,
  anchor_genes = NULL,
  lcs_threshold = 0.01,
  case_label = "Case",
  ctrl_label = "Ctrl",
  knn_list = NULL,
  graph_name = NULL,
  layer = "counts",
  remove_self_edges = TRUE,
  graph_symmetrize = c("none", "or", "max"),
  edge_weight_mode = c("binary", "weighted"),
  chunk_size = 5000,
  mc_cores = 1,
  min_samples_per_group = 1,
  verbose = TRUE
)
}
\arguments{
\item{sample_list}{Named list of expression inputs per sample. Each element
can be:
\itemize{
  \item A numeric or sparse matrix (genes x cells)
  \item A \code{Seurat} object
  \item A \code{BPCells} \code{IterableMatrix}
}}

\item{group_info}{Named character vector: sample name -> group label
(e.g. \code{c(s1="Case", s2="Ctrl")}). Must cover all names in
\code{sample_list}. Alternatively, an unnamed vector in the same order.}

\item{lr_db}{LR database. Default: \code{lr_pairs_human}.}

\item{rank_threshold}{Per-cell REO anchor quantile. Default: \code{0.5}.}

\item{anchor_genes}{Optional gene universe for REO anchors. Default \code{NULL}
uses all genes in each sample.}

\item{lcs_threshold}{Minimum LCS for "active" call. Default: \code{0.01}.}

\item{case_label}{Label for case group. Default: \code{"Case"}.}

\item{ctrl_label}{Label for control group. Default: \code{"Ctrl"}.}

\item{knn_list}{Optional named list of KNN matrices (cells x cells),
one per sample. If \code{NULL}, Seurat graphs are used when available,
otherwise global LCS mode is used.}

\item{graph_name}{Optional Seurat graph name passed to
\code{IdentifyLogicConsensus()}, for example \code{"RNA_nn"}.}

\item{layer}{Seurat assay layer/slot for REO conversion. Default: \code{"counts"}.}

\item{remove_self_edges}{Remove KNN self-loops before LCS scoring. Default: \code{TRUE}.}

\item{graph_symmetrize}{How to symmetrize KNN/SNN graphs before scoring.}

\item{edge_weight_mode}{\code{"binary"} or \code{"weighted"}.}

\item{chunk_size}{Cells per chunk for REO computation. Default: \code{5000}.}

\item{mc_cores}{Number of parallel cores. Default: \code{1}.
Set > 1 on Unix/macOS only.}

\item{min_samples_per_group}{Minimum number of non-missing samples per group
required by \code{CompareLogicGroups}. Default: \code{1}.}

\item{verbose}{Logical. Default: \code{TRUE}.}
}
\value{
A list of class \code{LogicCommMulti} with:
  \describe{
    \item{lcs_list}{Named list of LCS vectors (one per sample)}
    \item{comparison}{A \code{\link{CompareLogicGroups}} result data frame}
    \item{group_info}{Group assignments used}
    \item{params}{List of parameters used}
  }
}
\description{
One-stop function for multi-sample cell-cell communication analysis.
Runs \code{\link{calc_REO_matrix}} and \code{\link{IdentifyLogicConsensus}}
for each sample, optionally in parallel, then calls
\code{\link{CompareLogicGroups}}.
}
\details{
Parallel processing is handled via the \strong{parallel} package (base R).
On Unix/macOS, \code{mc_cores > 1} uses \code{mclapply}. On Windows,
set \code{mc_cores = 1} (fork-based parallelism is unsupported on Windows).

Memory tip: for very large datasets (> 100k cells per sample), pass
BPCells \code{IterableMatrix} objects in \code{sample_list}; LogicComm
will stream them chunk by chunk.
}
\examples{
\dontrun{
library(LogicComm)

# 6 Seurat objects: 3 Case + 3 Ctrl
groups <- c(rep("Case", 3), rep("Ctrl", 3))
names(groups) <- names(sample_list)

result <- run_multisample(sample_list, group_info = groups, mc_cores = 4)
print(result$comparison)
plot_lcs_bubble(result$comparison)
}

}
\seealso{
\code{\link{CompareLogicGroups}}, \code{\link{plot_lcs_bubble}}
}
