% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{as_logiccomm_lr_db_from_cellchat}
\alias{as_logiccomm_lr_db_from_cellchat}
\title{Convert a CellChatDB object to a LogicComm LR database}
\usage{
as_logiccomm_lr_db_from_cellchat(
  cellchat_db,
  pathway_col = "pathway_name",
  annotation_col = "annotation",
  interaction_name_col = "interaction_name",
  source_label = "CellChatDB",
  make_unique = TRUE,
  include_modulators = TRUE
)
}
\arguments{
\item{cellchat_db}{A CellChatDB list with an \code{interaction} data frame and,
optionally, a \code{complex} data frame.}

\item{pathway_col}{Column name in \code{interaction} to use as pathway. The
CellChat default is \code{"pathway_name"}.}

\item{annotation_col}{Column name in \code{interaction} to use as annotation.
If absent, annotation is filled with \code{"CellChatDB"}.}

\item{interaction_name_col}{Column name in \code{interaction} to use as pair
identifier. If absent, identifiers are generated from ligand and receptor.}

\item{source_label}{Text stored in \code{annotation} when \code{annotation_col}
is absent.}

\item{make_unique}{Logical; if \code{TRUE}, duplicate \code{lr_pair} names are
made unique with \code{make.unique()}.}

\item{include_modulators}{Logical; if \code{TRUE}, preserve CellChatDB
agonist/antagonist/co-receptor fields and resolve them to list columns.}
}
\value{
A data frame with LogicComm columns \code{lr_pair}, \code{ligand},
  \code{receptor}, \code{ligand_genes}, \code{receptor_genes}, \code{pathway},
  and \code{annotation}.
}
\description{
Converts the list-like CellChat database object (for example
\code{CellChatDB.human}) into the compact LogicComm ligand-receptor schema.
The CellChat interaction table is expected at \code{cellchat_db$interaction};
complex names referenced by ligand or receptor are resolved against
\code{cellchat_db$complex} when present.
}
\examples{
cellchat_like <- list(
  interaction = data.frame(
    interaction_name = "L_R",
    ligand = "L",
    receptor = "R",
    pathway_name = "test",
    annotation = "toy",
    stringsAsFactors = FALSE
  )
)
lr_db <- as_logiccomm_lr_db_from_cellchat(cellchat_like)
head(lr_db)
}
\seealso{
\code{\link{all_lr_genes}}
}
